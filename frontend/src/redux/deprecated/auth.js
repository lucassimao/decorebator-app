export const SET_LOGGED_IN = "set logged in";

const reducer = (state = {}, action) => {
  switch (action.type) {
    case SET_LOGGED_IN:
      break;
    default:
      return state;
  }
};

export default reducer;
